resources:
  jobs:
    options_book_ingestion_job:
      name: options-book-ingestion
      tags:
        project_name: llmops-databricks-course-rsreeram17

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - ../dist/*.whl

      tasks:
        - task_key: ingest_options_book
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/01_options_book_ingestion.py
